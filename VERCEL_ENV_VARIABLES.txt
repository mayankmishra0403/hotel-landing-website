# COPY THESE TO VERCEL ENVIRONMENT VARIABLES
# Go to: Vercel Dashboard → Your Project → Settings → Environment Variables
# Set all to "Production" environment

# ===== INSTRUCTIONS =====
# 1. Open your local .env.local file
# 2. Copy each variable below from your .env.local
# 3. Go to https://vercel.com/dashboard
# 4. Select your project → Settings → Environment Variables
# 5. Add each variable (Name = left side, paste Value from .env.local)
# 6. Select "Production" for environment
# 7. Click "Save"
# 8. After adding all, go to Deployments tab and click "Redeploy"

# ===== CASHFREE (CRITICAL FOR PAYMENTS) =====
CASHFREE_APP_ID=<copy from .env.local>
CASHFREE_SECRET_KEY=<copy from .env.local>
NEXT_PUBLIC_CASHFREE_MODE=production

# ===== BASE URL =====
NEXT_PUBLIC_BASE_URL=https://edu-nova.tech

# ===== APPWRITE SERVER-SIDE =====
APPWRITE_API_KEY=<copy from .env.local>
APPWRITE_PROJECT_ID=<copy from .env.local>
APPWRITE_DATABASE_ID=<copy from .env.local>

# ===== APPWRITE CLIENT-SIDE (NEXT_PUBLIC_*) =====
NEXT_PUBLIC_APPWRITE_ENDPOINT=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_PROJECT_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_DATABASE_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_USERS_COLLECTION_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_BOOKINGS_COLLECTION_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_REVIEWS_COLLECTION_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_CONTACTS_COLLECTION_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_STORAGE_BUCKET_ID=<copy from .env.local>
NEXT_PUBLIC_APPWRITE_USER_PREFERENCES_COLLECTION_ID=<copy from .env.local>

# ===== QUICK REFERENCE =====
# Total variables needed: 17
# Critical for payments: CASHFREE_APP_ID, CASHFREE_SECRET_KEY, NEXT_PUBLIC_CASHFREE_MODE
# Critical for auth: All APPWRITE_* variables
# After adding all variables, MUST click "Redeploy" in Vercel!
