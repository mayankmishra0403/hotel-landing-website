# Hotel Ritam — Mobile (Expo)

Mobile companion app built with Expo Router. Tabs: Home and Rooms.

## Run

1. Install deps
   - From `mobile-app/`: `npm install`
2. Start dev server
   - `npm run start`
3. Open on device/emulator
   - iOS: `npm run ios`
   - Android: `npm run android`
